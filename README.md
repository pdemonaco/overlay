# Overlay

Personal Gentoo overlay I use for some basic testing and development.

## Installation (eselect repository)

Add the overlay's repository list xml file and add it to the repository.

```bash
sudo eselect repository add pdemon git https://github.com/pdemonaco/overlay.git
```
